package mui.material.card;

@:enum abstract CardHeaderClassKey(String) to String {
	var Root = "root";

	var Avatar = "avatar";
	var Action = "action";
	var Content = "content";
	var Title = "title";
	var SubHeader = "subheader";
}
