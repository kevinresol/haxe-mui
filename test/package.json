{
  "name": "haxe-material-ui-tests",
  "scripts": {
    "init:4.0.x": "haxelib newrepo && haxelib install --always --skip-dependencies --quiet install.hxml",
    "init:3.4.7": "haxelib newrepo && haxelib install --always --quiet install.hxml",
    "test": "haxe test.hxml"
  },
  "dependencies": {
    "@mui/icons-material": "^5.10.9",
    "@mui/lab": "^5.0.0-alpha.107",
    "@mui/material": "^5.10.13",
    "jsdom": "15.2.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
